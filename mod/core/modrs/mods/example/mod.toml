[mod]
name = "example"
version = "0.1.0"
description = "Example mod — demonstrates the full ModApi pattern"

[build]
targets = ["native", "wasm32-unknown-unknown"]

[routes]
hello = "Say hello"
echo = "Echo back params"
status = "Get mod status"
time = "Get current timestamp"
transform = "Transform input data"
