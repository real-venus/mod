networks:
  default:
    external: true
    name: modnet
services:
  dev:
    container_name: dev
    deploy: {}
    entrypoint: bash -c "m serve port=50116 key=dev remote=0 mod=dev"
    image: mod:latest
    ports:
    - 50116:50116
    restart: unless-stopped
    shm_size: 100g
    volumes:
    - ~/mod:/root/mod
    - ~/.mod:/root/.mod
    - ~/mod/mod/orbit/dev:/root/mod/mod/orbit/dev
    working_dir: /root/mod/mod/orbit/dev
version: '3.8'
