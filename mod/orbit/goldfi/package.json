{
  "name": "goldfi-contracts",
  "version": "1.0.0",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:local": "hardhat run scripts/deploy.js --network ganache",
    "deploy:testnet": "hardhat run scripts/deploy.js --network base_sepolia"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "@openzeppelin/contracts": "4.9.3",
    "dotenv": "^16.3.1",
    "hardhat": "^2.19.0"
  }
}
